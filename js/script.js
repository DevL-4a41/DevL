/*const setContainerWidth = () =>
{
  let elem = document.querySelector('body');
  let wd = window.getComputedStyle(elem, null).getPropertyValue("width");
  
  let container = document.querySelector("#container");
  let str = wd;
  var matches = str.match(/(\d+)/);
  let new_value;
  if (matches)
    new_value = matches[0];
  container.style.width = new_value+"px";
}*/

